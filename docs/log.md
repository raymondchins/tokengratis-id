# tokengratis.id — Build Log & Learnings

> **Append-only.** Numbered learnings + key technical decisions. NEVER renumber existing entries.
>
> **Current state lives in `STATE.md`** (lighter, read more often). This file = the archive.

---

## How to add a learning

1. Append the next-numbered entry at the end.
2. Format:
   ```
   ### N. <Short title>

   **Context:** what triggered this (incident, feature, debug session).

   **Learning:** what we now know.

   **How to apply:** when this pattern recurs, do X.
   ```

---

## Numbered learnings

### 1. Schema harus ngikut apa yang sumber BENERAN punya, bukan apa yang "ideal"

**Context:** PRD awal nge-spec field CC-required / phone-required / akses-Indonesia / freeQuota / offerType. Pas isi data, hampir semua jadi "Unknown" → UI tabel jelek (kolom kosong di mana-mana). Riset 3 sumber utama (cheahjs, mnfst, aicredits) konfirmasi: ZERO sumber yang track field itu secara terstruktur — paling banter ada di prosa.

**Learning:** Mendesain schema dari "apa yang pengen ditampilin" (wishlist) ketabrak realita sumber. Yang reliable cuma: provider, model, context, modality, rate limit, signup url. Field aspirasional yang ga ada sumbernya = "Unknown" generator = anti-pattern UI + lawan prinsip anti-halusinasi.

**How to apply:** Sebelum bikin field/kolom, cek dulu sumber NYEDIAIN data itu terstruktur apa ga. Kalau cuma kadang muncul di prosa → simpan utuh sebagai `description`, JANGAN bikin kolom boolean/tristate yang mayoritas "Unknown". "Drop the column" > "show Unknown".

**Relevansi (data-modeling):** schema follows source reality, not wishlist — berlaku buat aggregator/scraper manapun.

### 2. Cross-source references HARUS keyed by source label, jangan positional/first-match

**Context:** Saat implementasi openrouter authoritative-trim (drop model openrouter dari group mnfst kalau ga ada di live API), code cari "grup pertama yang ada di partialGroups" → ternyata tangkap group mnfst, bukan live API openrouter. Hasil: 19 model openrouter live ke-drop salah. Nyari source reference pakai `partialGroups.indexOf(sourceLabel)` ambil index pertama → itu group mnfst. Fix: capture reference explicitly `partialGroups.findIndex(g => g === sourceLabelToMatch)`.

**Learning:** Kalau multi-source aggregator, cross-reference antar group/source WAJIB by **key/label, bukan positional first-match**. Positional reference brittle, makan waktu debug.

**How to apply:** Saat nyari sumber specific di list partai/groups (model list, provider group, format variant), selalu pakai `.find()` / `.findIndex()` dengan explicit label match, JANGAN `.indexOf()` first occurrence. Apply ke future multi-format sumber juga (kalau cheahjs + amardeeplakshkar keduanya masuk).

**Relevansi (data-aggregation):** keyed reference > positional — berlaku buat parser manapun yang merge multi-format.

### 3. Pilih sumber by machine-readability, bukan popularity

**Context:** cheahjs paling terkenal/aktif tapi markdown-only (butuh scraping rapuh). mnfst (4.7k⭐) punya `data.json` bersih + terstruktur.

**Learning:** Buat aggregator maintenance-nol, sumber dengan JSON/structured output >> sumber populer tapi markdown. Scraping markdown = brittle + maintenance.

**How to apply:** Saat milih sumber data, prioritas: (1) ada JSON/API publik? (2) maintained? (3) coverage. Anchor ke yang structured, sisanya cross-ref belakangan.

---

## Key technical decisions

_(record major architecture / API contract / schema choices with rationale)_

- **2026-06-01 — Anchor data = mnfst `data.json`:** Pilih mnfst/awesome-free-llm-apis sebagai sumber utama (JSON bersih, 24 provider, model-level). Alternatif (cheahjs/amardeeplakshkar = markdown, aicredits = llms.txt prosa) ditunda — butuh parser. Tradeoff: coverage lebih kecil tapi reliability + maintenance ~nol menang.
- **2026-06-01 — Buang field CC/HP/akses-Indonesia:** Ga ada sumber yang track terstruktur → selalu "Unknown". Indonesia-first di-reframe jadi audience/bahasa, bukan klaim filtering akses. Kalau mau hidupin lagi → WAJIB layer editorial manual (lawan maintenance-nol, jadi ditahan).
- **2026-06-01 — Desain pivot ke light/paper neutral (getaiperks-style):** Dari dark/oren brand palette → light paper + Georgia serif + Inter + accent hijau/ungu. Oren di-pause (token-based, revert 1-2 baris). Rationale: ikut UI language getaiperks.com (light/paper editorial).
- **2026-06-10 — Sumber ke-4: openrouter live API + authoritative-for-self:** Pilih openrouter sebagai sumber ke-4 (live API, public, no auth) filter `:free` only. Emit single provider `openrouter`, authoritative buat model list sendiri (post-merge, pangkas ke model beneran live → entri komunitas stale auto-drop nightly). Rationale: live source = freshest, authoritative pattern mencegah staleness dari crowd-sourced entry.
- **2026-06-10 — Enrichment layer (models.dev) untuk gap-fill context/maxOutput:** Post-merge, exact-key match dari models.dev (`api.json`) buat nyumbuhin context/maxOutput field yang masih null (3 sumber utama sering incomplete). models.dev BUKAN sumber free-tier (bukan buat discovery), cuma metadata teknis. Exact-match only (ga overwrite existing), append SourceRef. Rationale: reduce "null" value di listing, improve sort-by-context akurasi; best-effort (error → providers tetap, ga jatohin pipeline).
- **2026-06-10 — LLM fallback dual-backend (ANTHROPIC_API_KEY / CLAUDE_CODE_OAUTH_TOKEN):** Kalau sumber unstructured (freellm HTML / cheahjs markdown) gagal regex sanity floor (markup drift), re-fetch + re-parse via LLM (Claude Haiku, verbatim-only prompt, structured JSON output). Dual backend: (1) raw `ANTHROPIC_API_KEY` (API billing per-token), (2) headless `claude` CLI via `CLAUDE_CODE_OAUTH_TOKEN` atau login `claude` CLI lokal (kuota subscription, bukan API billing). Aktif hanya kalau salah satu ada, otherwise fallback OFF (sync jalan normal). Hasil LLM tetap lewat sanity floor + smoke + diff guard. Rationale: automation rescue untuk markup drift (common risk saat HTML-dependent sumber); dual backend akomodasi CI (ga ada cred) + dev lokal (subscription); cost-aware (billing + quota tradeoff).
- **2026-06-10 — Rolling baselines (data/source-baselines.json):** Auto-recalibrate tiap sync sukses, store last-known-good per sumber (provider count, model count, sanity metric). Guard diff >25% → block push, manual review. Rationale: early warning untuk catastrophic data loss / sumber collapse; audit trail buat correlation saat sumber jatuh.

---

## Update protocol

- **Project-specific learning** → write here as the next-numbered entry.
- **Decision made** → append to Key Technical Decisions with date + rationale.
- **State change** (phase shift, source added/removed, blocker change) → update `STATE.md`, not here.
- WHY 2026-06-18: accept upstream contraction 27→20 providers (force ALLOW_DATA_SHRINK once) — verified 8 providers genuinely delisted by ALL sources 2 days running; mnfst clean JSON confirms not a parse flake. Aggregator mirrors sources; guard was preserving stale entries. Floor kept at 21.
- WHY 2026-07-17: source-expansion scan (31 candidates, adversarial eval) — HOLD, current 4 sources sufficient. All 6 endpoints live; 0 real dupes (audit-dupes clean). Only marginal add = 0xzr/freellmpool (TOML) but real net-new providers = just Pollinations + LongCat (report over-counted; llm7/kilo/opencode/ovh already covered). mnfst anchor stale since 2026-06-15, no clean fresher replacement — lean on openrouter+freellm for freshness. Backlog watch: CYBIRD-D (CN, messy prose), nejib1/Free-LLM. Skip amardeeplakshkar (stale Mar-2026) + aicredits.dev (no model-level data).
- INCIDENT 2026-07-17: nightly sync GAGAL 2 malam (15 & 16 Jul), data live beku di 14 Jul. Root cause = freellm.mjs ga dedup model per-provider (line ~253) → freellm.net kadang render baris dobel → parser over-count 299→618→885 → rolling baseline ratchet ke 885 (floor 442) → freellm asli ~351 di bawah floor → source di-skip tiap malam → 4 provider freellm-only (agnes-ai/chutes-ai/glhf-chat/xai) hilang → snapshot guard blokir push. Parser SEHAT (live parse 351 normal), murni baseline inflated. Fix: (imm) rebase baseline freellm ~351 / manual run allow_data_shrink=true; (durable) dedup model per-provider di freellm.mjs.
- HARDENING 2026-07-19: code-review quick-wins batch (9 fixes, 6-agent parallel review). Dedup model-per-provider added to ALL adapters (cheahjs/mnfst/openrouter) — same class as freellm incident; cheahjs had 2 real dupes (130→128). Baseline growth cap in updateBaselines (Math.min(new, prev*1.5)) so no source's over-count can permanently inflate the floor. Fetch timeouts on mnfst/cheahjs/enrich/llm-fallback (openrouter/freellm already had). Favicon fetch fail now keeps existing on-disk PNG instead of nulling logo. Snapshot prev Array.isArray guard (non-array JSON no longer crashes guard). gapFillModel modality wrapped in cleanModality. LLM re-fetch mirrors adapter UA/Accept headers. Deleted dead DIRECTORY_GRID export. SKIPPED the freeLimit trial-window loosening — it risks overclaiming a trial credit as standing (anti-halusinasi rule #1).
- SIMPLIFY 2026-07-19: dedup batch (2 parallel agents, zero overlap). Frontend: pageNumbers→lib/pagination.ts, shared <Pagination>/<Chip>/<EmptyDataPanel>/<NoResultsPanel>, opensource search svg→SearchIcon (Directory & Opensource clients no longer copy-paste). Pipeline: decodeEntities/textOf→normalize.mjs shared by freellm+cheahjs (FIXES cheahjs mangling &#39;/&nbsp; — preventive, no current data affected), GENERIC_MODELS_PATTERN single-source, countOf helper (3 dup sites), gapFillModel field-loop (cleanModality preserved), byPriorityKey factory, enrich 3 try/catch→1, dead lib/data.ts re-export deleted, ctxNum single-source lib/ctxnum.mjs (TS re-export + normalize.mjs import — dup copy deleted). Sync output identical pre/post (24/400) = behavior-preserving proof. Pending decisions (untouched): NewsletterForm dead code, ts-morph devDep, freellm fetchHtml→fetch.
- CLEANUP 2026-07-19: 3 pending decisions executed (Ray ACC). NewsletterForm.tsx deleted (0 consumer; route /api/subscribe tetap dormant, recreate form dari git history kalau newsletter dihidupin). ts-morph dropped dari devDeps (agentmap bawa sendiri 28.0.0, 0 direct import, agentmap verified jalan). freellm fetchHtml hand-rolled https (~45 baris, tanpa timeout) → built-in fetch + UA/Accept + AbortSignal.timeout(20s), live-verified identik 23 provider/347 model.
- TRAFFIC 2026-07-19: SEO/GEO wave shipped (5 impl agents + 4 research agents paralel, riset dulu baru build). Halaman 36→99: +55 /model/[slug] cross-provider (≥2 provider, disclaimer versi-bisa-beda), +5 facet /gratis/{vision,image,code,video,audio} (gated ≥3 provider, text/embeddings/reranking sengaja skip), +/changelog (diff nightly otomatis dari sync.mjs), +llms.txt & llms-full.txt (speculative keep — bukti lemah). Provider pages: title keyword-first, FAQ auto dari data (field absen → pertanyaan skip), BreadcrumbList+ItemList JSON-LD. IndexNow ping wired di sync.mjs (real ping 202). Robots eksplisit allow AI crawlers. Roadmap lengkap + skip-list (400 per-model pages, 276 comparison = scaled-content trap) di docs/TRAFFIC-ROADMAP.md. Distribusi (butuh Ray) = lever terbesar: post kanal utama > Threads listicle > outreach WPU/Kelas Terbuka.
- WHY 2026-07-19: flip primary domain www→apex di Vercel (apex primary, www 308 ke apex) — semua canonical/sitemap/OG/robots nunjuk apex tapi serving-nya di www (apex 307 ke www) = sinyal canonical muter + GSC "Couldnt fetch" sitemap. Fixed via PATCH project domains API. GSC domain-property + TXT DNS verification kepasang hari yang sama (record di Vercel DNS), sitemap 87 URL submitted.
- INCIDENT 2026-07-22: nightly gagal 2 malam LAGI (19 & 20 Jul) — regresi dari cleanup b46f7d0 sendiri. freellm.net 403 ke built-in fetch dari GitHub Actions (WAF fingerprint undici: accept-encoding otomatis dll), https.get lama aman berminggu-minggu dari CI yang sama; lokal (IP residential) dua-duanya jalan = false confidence. Fix: revert fetchHtml ke https.get client (+ socket timeout), komentar JANGAN-modernisasi dipasang di file. Learning: swap HTTP client di adapter WAJIB divalidasi lewat run CI beneran, bukan cuma lokal. Known gap: LLM-fallback re-fetch freellm juga pakai fetch → bakal 403 di CI juga; belum diubah (secondary layer, adapter utama udah fixed).
- WHY 2026-07-25: docs/PRODUCT-ROADMAP.md disusun dari riset 5-agen paralel (kompetitor, pasar ID, permukaan teknis, supply-side kredit, model bisnis+regulasi). Keputusan besar: (a) reposisi dari direktori ke Pilih→Pasang→Jaga karena format direktori sudah komoditas (≥8 clone, cheahjs 28k★) dan tidak punya retention loop; (b) gateway/proxy free-tier DITOLAK permanen — larangan eksplisit di OpenRouter ToS §7.4, Groq SA §3.2/§6.3, Mistral, Gemini Terms, PLUS rate limit Groq per-organisasi bikin shared key 429 berjamaah, jadi gagal legal sekaligus arsitektur; (c) ekspansi ke free tier hosting/DB/startup-credit diperlakukan sebagai KURASI mingguan entity terpisah, bukan adapter nightly, karena riset membuktikan nol sumber machine-readable di domain itu (free-for-dev pun tidak encode angka kuota); (d) jalur startup satu-satunya yang kredibel = jasa top-up saldo IDR ke akun milik user sendiri (bukan proxy/bukan account-sharing), harus brand terpisah karena aset situs ini justru kepercayaan non-komersial.
- SHIP 2026-07-25: Fase 1+2+4+5 roadmap dieksekusi (7 agen paralel, ownership file disjoint, nol tabrakan tulis). BARU: /pilih (wizard model), /fallback (generator rantai multi-provider: Vercel AI SDK/LiteLLM/TS/.env), SetupPanel di tiap /provider/[slug] (5 target snippet, 5/24 provider baseUrl null -> fallback jujur, BUKAN base URL karangan), /modal-gratis + 54 halaman detail (entity Offer terpisah di lib/offer-types.ts, kurasi manual mingguan karena riset buktikan NOL sumber machine-readable di domain ini), /feed.xml RSS, /api/providers + /api/models (CORS terbuka, static), packages/tokengratis (CLI + MCP server stdio JSON-RPC, ZERO dependency, hand-rolled). Halaman 87 -> 143 URL sitemap. Fase 3 (status ping) SENGAJA dilewat: butuh 8-10 API key atas nama Ray, ga bisa didelegasi. Telegram+email di-skip atas permintaan Ray. WHY entity Offer terpisah: skema LLM (context/modality/rateLimit) ga muat buat '10 GB storage'/'$10.000 credit' -> dipaksa satu = kolom kosong bertaburan, persis yang dilarang aturan #1. WHY facets turunan bukan boolean bebas: pola sama kayak Provider.modalities yang di-derive dari string sumber, jadi facet ga bisa jadi tempat buang opini.
- HARDENING 2026-07-25: guard berbasis BENTUK NILAI, bukan jumlah (5 agen paralel). Pemicunya insiden freellm hari ini: kolom ketuker bikin 216/398 model salah selama berminggu-minggu, dan SEMUA guard lama lolos karena semuanya ngukur KUANTITAS (row count, sanity floor, id churn) sementara yang berubah cuma ARTI kolom. Ketahuan gara-gara manusia ngeliat 'context=81' di output CLI — bukan guard. BARU: scripts/lib/shape-guard.mjs (8 aturan ratio-based, fatal cuma kalau sistemik >50% dgn sampel >=20; nangkep bare-int<1000 = signature kolom Score, currency, tanggal, sisa markup/entity, context===rateLimit, modality tak dikenal, URL invalid, all-null collapse=warn). diff-guard dapet Rule 6 field-value churn (context/maxOutput/modality/rateLimit + description/baseUrl/freeLimit/maxContext), null->value dari enrich SENGAJA dikecualikan biar enrichment ga bikin flap. cheahjs map kolom by nama header (dulu 2-td positional). mnfst+openrouter dapet validasi shape + fill-rate guard (openrouter paling rawan: ga punya baseline di source-sanity, jadi hasil 0-model bisa lolos senyap). sync.mjs jalanin checkShape per-sumber DAN di hasil merge; fatal per-sumber = skip sumber itu, fatal di merge = abort tulis + exit 1. Notifikasi nol-setup: workflow udah punya step buka/update GitHub issue on failure (di-reuse, bukan bikin baru), sekarang isinya baca data/sync-report.json. BUKTI end-to-end lawan data asli (c95ee6d pra-fix vs 2f04f70 pasca-fix): shape-guard FATAL ratio 0.63 sample [81,55,52]; diff-guard BLOKIR 62.4% churn context + 41.7% maxContext; data pasca-fix BERSIH (nol false-positive). 94 selftest, semua masuk npm test. WHY ratio+min-sample bukan absolute: guard yang teriak di data bagus bakal dimatiin orang, dan itu lebih buruk daripada ga ada guard.
- WHY 2026-07-27: audit UI/UX (9 agen paralel + ukur DOM live di production) nemu 3 bug scroll horizontal di mobile yang ga ada satu pun ketangkep tsc/build/CI — semuanya cuma keliatan kalau viewport-nya beneran 375. Akar masalahnya SATU pola yang sama: elemen ga bisa nyusut. (a) FilterBar baris mobile 402px di container 343px tanpa flex-wrap; (b) /pilih grid item default `min-width:auto` bikin track ke-resolve ke min-content kartu (379.5px) padahal container 343px — ini yang paling ga obvious, kontennya sendiri cuma butuh 183px; (c) /gratis/[modality] `min-w-[640px]` ke-apply kebalik: aktif di HP, mati di desktop (`md:min-w-0`), padahal sibling header-nya `hidden` jadi keliatan aman — tombol "Lihat" kedorong ke x~657 alias di luar layar, task-blocker beneran. Learning: guard build ga bisa liat layout; bug responsive cuma ketangkep sama pengukuran DOM di viewport target, dan `min-w-[...]` WAJIB di-gate `md:` kalau baris-nya punya layout mobile sendiri — pola yang bener udah ada di OpensourceClient.tsx (min-w cuma di elemen `hidden md:grid`), tinggal ditiru.
- SHIP 2026-07-27: sisa backlog audit UI/UX dieksekusi (7 agen paralel, ownership file disjoint, nol tabrakan tulis) — URL state di 5 permukaan (/, /pilih, /fallback, /modal-gratis, /opensource), menu mobile di Navbar, target sentuh 44px, heading beneran, focus ring, aria-live/aria-current, plus konvergensi H1/CTA/container/ring.
- WHY 2026-07-27: URL state SENGAJA ga pakai useSearchParams — situs ini 100% SSG dan useSearchParams maksa dynamic rendering (atau butuh Suspense yang ngerepotin). Polanya: state start dari default (server & client render identik = no hydration mismatch), baca URL sekali di effect pas mount, tulis balik pakai history.replaceState (bukan pushState, biar back button ga penuh sampah), semua param divalidasi ke data asli biar link basi di-drop diam-diam bukan bikin crash. Dibuktiin di build output: rute-rute itu tetep ○ (Static), 159 halaman prerender.
- INCIDENT 2026-07-27: menu mobile Navbar ship dalam keadaan MATI TOTAL di e87142d, ketangkep cuma gara-gara ngeklik tombolnya beneran di production. Semua sinyal "aman" hijau: tsc bersih, next build hijau, CI sukses, 159 rute prerender. State-nya juga bener semua (aria-expanded flip, label ganti, Escape & klik-luar nutup) dan class-nya kebalik dengan bener (`visible max-h-96 opacity-100`), <ul> di dalemnya beneran 195px — TAPI computed style panel nyangkut di visibility:hidden/opacity:0/max-height:0px, dan inline !important pun ga ngalahin. Utility-nya sendiri normal (dites pakai elemen probe). Ga dikejar akar cascade-nya; POLANYA yang salah. Fix: panel di-mount/unmount, bukan di-toggle lewat class. Learning: (1) nyembunyiin konten di balik transisi class itu rapuh — transisi ga jalan di tab background/headless render, dan sekali computed style nyangkut fiturnya mati senyap tanpa error; (2) build hijau ≠ fitur jalan, komponen interaktif WAJIB diklik di halaman jadi; (3) ini kelas bug yang sama kayak 3 bug scroll horizontal kemarin — semuanya cuma keliatan lewat pengukuran DOM di viewport target, nol ketangkep sama compiler.
- WHY 2026-07-27: baris direktori BERHENTI jadi satu `<a>` besar. Pemicunya kewajiban nampilin `SourceLine` + link `Ga ada di sumber` di tiap baris — dua-duanya anchor, dan `<a>` di dalam `<a>` itu markup invalid yang di-un-nest senyap sama browser (link barisnya jadi rusak, bukan error). Dua alternatif ditolak: `display:contents` di Link (ngilangin semantik link di a11y tree beberapa browser) dan stretched-link `absolute inset-0` (matiin seleksi teks DAN nutupin link sumber yang justru mau diekspos). Gantinya navigasi pindah ke 2 link nyata: blok identitas provider + tombol `Lihat`; `hover:bg-ink/40` + `focus-within:bg-ink/40` yang bikin barisnya tetep reaktif. Ongkos yang diterima: tab stop per baris 1 -> 2 (+ link sumber yang toh bakal jadi tab stop di struktur apapun), dan di HP ilang "tap di mana aja".
- WHY 2026-07-27: state chip terpilih ga bisa dibenerin lewat FILL. Diukur di production fill lama composite ke rgb(232,230,221) = 1.25:1 lawan chip putih, 1.09:1 lawan halaman — jauh di bawah 3:1 WCAG 1.4.11. Matematikanya: fill tonal baru nyampe 3:1 kalau relative luminance <=0.30, dan itu artinya chip HITAM — langsung nabrak One Black Rule + bikin 9 chip hitam sebaris. Jadi 1.4.11 dibawa border `mute` solid (4.85:1 lawan kertas, 5.55:1 lawan chip putih) + glyph `✓`; `--color-ink-sel` cuma alat scanning. Turunannya: `text-mute` di atas ink-sel = 3.99:1 (gagal AA buat angka 11px) -> counter chip aktif pakai `text-fog/70`.
- WHY 2026-07-27: chip filter BALIK ke wrap di semua breakpoint, ngebatalin keputusan scroll-horizontal 2026-07-25. Scroll-nya sendiri emang bener nyelesain bug overflow halaman, tapi diukur di production dia nyembunyiin 5 dari 9 filter (769px konten di kotak 351px) DAN bikin clear-all `Semua` nyangkut di left:-402px — user bisa mendarat di 3 hasil tanpa tau kenapa dan tanpa jalan keluar yang keliatan. Wrap ngasih 9 dari 9 facet keliatan, ongkosnya ~3 baris chip di HP. Bonus: `-m-1 p-1` ikut dibuang karena alasannya (overflow-x:auto maksa sumbu Y jadi auto lalu nge-clip focus ring) udah ga ada.
- WHY 2026-07-27: sort "jatah gratis terbesar" DITOLAK, diganti "Ada info gratis". `freeLimit` itu teks bebas dari 4 sumber dengan satuan ga sebanding ("1M token/hari" vs "$10 credit" vs "10,000 Neurons/hari") — ngerangkingnya = ngarang perbandingan yang sumbernya ga nyediain, persis aturan #1. Yang derivable cuma "punya info gratis vs nggak", tie-break re-pakai array POPULARITY yang udah ada.
- SHIP 2026-07-27: eksekusi backlog critique UI/UX homepage (skor 25/40, 4 agen paralel, ownership file disjoint). `ProviderListItem` nambah `sources[]` (~4KB) supaya `SourceLine` bisa render di direktori — sebelumnya prinsip #1 PRODUCT.md ("tiap data nampilin sumbernya") nol dieksekusi di permukaan yang paling banyak dilihat. Em dash mati di DirectoryClient + ModelsTable (sel kosong buat grid/tabel, elemen diilangin buat flow layout, `Ga ada di sumber` + link sumber buat kolom GRATIS). 49 CTA hitam -> secondary; CTA navbar demote khusus di "/" (di rute lain tetep hitam karena di sana navigasi beneran). Navbar 3 target 40px -> 44px, translucency /90 -> /95 (baris tabel kebaca nembus pill). Chip dapet counter hasil (bikin semantik AND jelas sendiri, ga perlu tooltip), `Semua` jadi tombol reset beneran (dulu `aria-pressed` bohong ke AT), plus `role="group"` + baris "N filter aktif · Reset" permanen. Kolom GRATIS 140 -> 180px, diambil dari KEMAMPUAN (grid ikon fixed 80px, 130px-nya ruang mati) bukan dari CATATAN yang sekarang nampung SourceLine.
- WHY 2026-07-27: SourceLine DIBUANG dari baris direktori (baru dipasang pagi ini juga). Prinsipnya bener — prinsip #1 minta tiap data nunjukin sumbernya — tapi eksekusinya di tabel salah: 24 baris x 3-4 nama sumber bikin tiap row ~3x lebih tinggi, dan atribusinya HAMPIR IDENTIK di semua baris (mnfst + freellm + cheahjs), jadi rasio sinyal-ke-tinta-nya jelek banget. Kuitansi lengkap tetep ada di /provider/[slug]; di direktori yang tinggal cuma yang beneran per-baris dan informatif: link "Ga ada di sumber" di kolom GRATIS. Catatan: baris tetep BUKAN satu <a> — link itu masih anchor, jadi alasan de-anchor-nya belum ilang.
- WHY 2026-07-27: logo provider null itu DUA kelas bug beda, bukan satu. (a) `domainOf(url, baseUrl)` balik null buat provider yang sumbernya (freellm.net) nol URL — 100% field url & baseUrl null di 23 provider, ke-flag shape-guard tapi emang legit. Ditambah `domainFor()`: nama yang HARFIAH hostname ("Chutes.ai" -> chutes.ai, "Glhf.chat" -> glhf.chat) itu di-parse bukan ditebak, plus DOMAIN_ALIASES manual (isinya cuma xai -> x.ai) yang sengaja dibatasin buat logo doang karena favicon bukan field yang dipakai user buat mutusin. (b) download favicon nol retry: 24 fetch bareng ke satu layanan, sekali gagal logonya ilang sampai sync berikutnya kebetulan sukses (aion-labs: domain bener, PNG ga pernah nyampe disk). Sekarang 3x percobaan + backoff, timeout 5s -> 8s. Hasil 5 -> 3 tanpa logo. Sisanya BUKAN bug kita: glhf.chat & aionlabs.ai di-404 sama layanan favicon Google (balikin globe generik 726 byte, md5 identik b8a0bf372c76 — ketahan `!r.ok`), dan agnes-ai emang nol domain di mana-mana. Fallback DuckDuckGo ga di-ship karena hostnya keblok dari sandbox = ga bisa dibuktiin jalan.
- INCIDENT 2026-07-27: `npm run sync` DIBLOK snapshot guard — github-models anjlok 39 -> 13 model (-66.7%, ambang 40%) plus id churn 66.7% (overlap cuma 33.3%). Last-known-good tetep live, PERSIS yang diharepin. Kemungkinan besar ini juga penyebab workflow nightly-data-sync merah. SENGAJA ga di-ALLOW_DATA_SHRINK=1: waktu itu lagi benerin ikon, dan maksa ngelewatin guard buat kosmetik = persis kelakuan yang bikin guard-nya dimatiin orang. Backfill logo dikerjain terarah dulu (cuma field domain+logo, nol re-fetch sumber) -> diff 5 baris, 397 model utuh. SETELAH itu baru akar masalahnya dikejar dan ketemu: BUKAN parser patah. README cheahjs sekarang NOL sebutan "github" (dicek pakai grep, bukan diliat sekilas), dan commit upstream 2026-07-25 judulnya harfiah "Remove GitHub Models ahead of its retirement" — layanannya emang mau dipensiunkan. 26 model yang ilang itu persis kontribusi cheahjs (id gaya dash: openai-gpt-5-mini, phi-4-reasoning, dst); mnfst (10) + freellm (13) masih nyumbang. Jadi ALLOW_DATA_SHRINK=1 di sini SAH — persis alur yang guard-nya minta: blokir dulu, manusia verifikasi, baru force. Sync ulang: 25 provider / 375 model, kilo-gateway masuk sebagai provider baru (juga cocok sama commit upstream), nol provider hilang. Learning: pesan guard "possible source corruption" ternyata bisa berarti "sumbernya bener-bener ngapus", dan yang mbedain cuma satu langkah — baca git log repo sumbernya, bukan cuma README-nya.
- INCIDENT 2026-07-27: nightly-data-sync GAGAL 8 dari 25 run terakhir (32%), dan SEMUA 8 sebabnya SATU hal: snapshot guard trip lalu abort seluruh write. Nol masalah infra, nol npm/network error. Empat insiden, tiap insiden PAS 2 malam berturut-turut: 07-07/08 modelscope 50->27 (-46%); 07-15/16 agnes-ai+chutes-ai+glhf-chat hilang; 07-19/20 tiga provider sama hilang lagi; 07-25/26 github-models 39->13. Tiga provider yang hilang itu persis provider yang sumbernya CUMA freellm.net — sekali freellm nge-render ga utuh, mereka lenyap. Pola "selalu 2 malam" itu petunjuk kunci: guard bandingin ke data terakhir yang KE-COMMIT, dan pas guard nge-block ga ada yang di-commit, jadi baseline-nya ga pernah maju dan anomali yang sama trip lagi besoknya sampai sumbernya kebetulan pulih.
- WHY 2026-07-27: guard nightly diubah dari "abort semua-atau-nol" jadi KARANTINA PER-PROVIDER (scripts/lib/quarantine.mjs). Cacat desain lamanya: satu provider goyang bikin 24 provider lain yang datanya segar ikut kebuang, situs basi 2 malam, dan cuma pulih kalau manusia turun tangan. Prinsip yang dipakai: KORUPSI ITU SEMENTARA, KENYATAAN ITU MENETAP — dan yang mbedain cuma WAKTU, sesuatu yang guard lama ga punya karena cuma ngeliat satu run. Sekarang: anomali per-provider (hilang/nyusut/id-churn) bikin provider ITU pakai record last-known-good sementara sisanya ditulis segar dan run tetep HIJAU; anomali SAMA 3 run berturut-turut = diterima otomatis; sumber pulih sebelum 3 run = karantina dibersihin. Carry-forward JUJUR by construction: record lama dibawa apa adanya jadi syncedAt-nya GA maju, situs otomatis nampilin "Disinkron [tanggal lama]" — ga ada klaim kesegaran palsu. WHY tetep ada yang fatal: error GLOBAL (total model anjlok, minProviders floor, field-churn Rule 6 alias detektor kolom-ketuker INCIDENT 07-25) + >30% provider anomali sekaligus = pola korupsi sistemik, karantina SENGAJA nolak nanganin. WHY ada batas umur 7 hari: tanpa itu anomali yang magnitude-nya flapping (13->20->8->25) bakal nge-reset streak terus, ga pernah nyampe 3, dan providernya di-carry-forward SELAMANYA alias basi diam-diam. Bug ini ketangkep sama selftest-nya sendiri (firstSeen awalnya ikut ke-reset tiap streak reset, jadi batas umurnya ga akan pernah nyala) — bukti kenapa selftest ditulis bareng fiturnya, bukan sesudahnya.
- SHIP 2026-07-27: karantina dibuktiin END-TO-END lawan sumber asli, bukan cuma unit test. Baseline di-set ke snapshot lama (github-models=39), terus `npm run sync` dijalanin 3x beneran: malam 1 HIJAU (carry-forward 1/3, github tetep 39 model dengan syncedAt 25 Jul, 24 provider lain segar, 401 model ditulis), malam 2 HIJAU (2/3), malam 3 HIJAU + DITERIMA OTOMATIS (github jadi 13, 375 model, quarantine.json balik kosong). Nol intervensi manual, nol run merah. Skenario yang sama persis bikin 2 run nightly MERAH minggu ini. 28 selftest quarantine masuk npm test.
- INCIDENT 2026-07-27: Ray masih ngerasa UI/UX membingungkan PADAHAL seluruh backlog critique homepage (25/40) udah ship & terverifikasi. Critique ulang di level PRODUK ngasih 22/40 — LEBIH RENDAH dari homepage-nya sendiri. Akar masalahnya: audit komponen secara struktural buta sama arsitektur informasi. Diukur di / pada 375x812: tinggi dokumen 7.272px (9 layar), SEMUA link header ke /pilih /modal-gratis /opensource `visible:false` (kelipat di hamburger), link tool pertama yang keliatan ada di footer y~7.019px alias 8,6 layar ke bawah, /fallback nol di header di SEMUA viewport, dan NOL link tool di dalam <main> (di-grep sendiri, bukan percaya agen). Heuristic terburuk: Consistency 1/4 — 5 permukaan daftar dengan 5 tata bahasa interaksi beda, jadi nol yang dipelajari di satu halaman kepake di halaman lain. Learning: kalau user bilang "masih bingung" setelah backlog komponen kelar, pindahin lensa ke IA/perjalanan — bukan ngulang audit komponen yang sama.
- WHY 2026-07-27: hero dapet 3 baris tugas ASIMETRIS, bukan 3 kartu seragam. DESIGN.md ngelarang "identical card grids" dan tiga kotak sama persis sebaris itu persis polanya. Bentuknya beda karena niatnya beda: "Cari sendiri" full-width (aksi default, tabelnya emang isi halaman ini), sementara /pilih + /fallback setengah-lebar dengan hierarki DIBALIK (pertanyaan mute dulu, aksi fog kedua) karena dua-duanya kondisional — cuma relevan buat sebagian pembaca. Nol elemen hitam: navbar CTA di "/" udah diturunin duluan, jadi jatah hitam halaman ini emang sengaja kosong.
- WHY 2026-07-27: navbar pindah dari md: ke lg:. Nambah "Rantai fallback" bikin 5 link, dan di 768px cuma ada ~360px buat baris link sementara 5 label butuh ~392px teks doang. Temuan sampingan yang lebih penting: 4 link yang LAMA pun sebenernya ga muat (~401px vs 360px) — flex-nya diem-diem meres selama ini. Badge NEW DIBUANG bukan dipindah: warnanya grass (hijau = free tier di sistem ini), jadi itu pemakaian aksen semantik buat hiasan, dilarang Semantic Accent Rule; plus di project maintenance-nol, badge "NEW" itu utang yang ga ada yang inget ngapus.
- WHY 2026-07-27: cap 5 di /pilih dipindah dari DALAM findWizardMatches ke pemanggil. Dulu `matches.length` yang UDAH kepotong dirender sebagai "N provider cocok", jadi halaman bilang "5 provider cocok" padahal 11 — pernyataan salah di situs yang jualannya kuitansi bukan klaim. Sekarang fungsi balikin semua yang cocok terurut, pemanggil yang motong, jadi label bisa jujur ("Menampilkan 5 teratas dari 11 yang cocok") + ada jalan keluar ke /?m=<modality>#direktori. Kriteria rankingnya juga ditulis eksplisit — nomor #1 tanpa alasan itu sendiri sebuah klaim.
- WHY 2026-07-27: chip "Ada info gratis" dikasih GRUP SENDIRI, bukan chip ke-10 di grup modality. Grup itu diumumin "Kemampuan" ke screen reader, jadi nyelipin predikat free-tier ke situ itu sekelas bohongnya sama `aria-pressed` di tombol reset yang barusan dibuang. Lagian temuan P0 yang dikubur jadi chip terakhir sama ga ketemunya kayak waktu dia jadi opsi ke-5 di <select>. Ongkos ~95px vertikal di 375px sengaja dibayar. Sort key `freeinfo` sekalian DIBUANG dari lib/filter.ts: sort cuma ngurut ulang, chip beneran motong, dan dua jalan buat satu kerjaan persis inkonsistensi yang lagi dibenerin — sekalian opsi sort balik ke 4, di bawah batas kerja ingatan.
- WHY 2026-07-27: sub-label periode di kolom GRATIS DITOLAK setelah ngecek string aslinya. Dari 12 nilai non-null: 7 udah nyebut periode inline ("1M token/hari", "100K kredit/bln"), 2 udah nyebut di kata ("Free (permanen)"), dan 3 GA PUNYA periode sama sekali ("2 RPM/IP" itu laju bukan jendela, "Free, no signup", "~22 model free"). Jadi sub-label bakal ngulang string buat 9 dan NGARANG periode yang sumbernya ga pernah nulis buat 3 — yang kedua langsung nabrak aturan #1. Nol comparability didapat, jadi nol chrome ditambah.
- SHIP 2026-07-27: eksekusi backlog critique produk (22/40, 8 unit, ownership file disjoint, nol tabrakan tulis). BARU: components/FilterBar.tsx bareng dipakai /, /pilih, /modal-gratis, /opensource (urutan render, label grup, baris "N filter aktif · Reset" identik) — FilterBar lama dihapus; 3 baris tugas di hero + "Rantai fallback" di navbar (link tool dari y~7.019px jadi ~600px); components/nav/DetailBreadcrumb.tsx (breadcrumb + back gabung satu kontrol, state dipulihin lewat Navigation API karena pushState ga pernah ngupdate document.referrer, fallback ke referrer lalu ke /#direktori yang server-rendered); 132 link ke 51 halaman /model/* dari 20 halaman provider (nol yatim); /gratis/* em dash mati + 11 CTA hitam jadi sekunder + SourceLine level-halaman; label CTA seragam "Lihat" di semua permukaan. 156 halaman, semua rute tetap static.
- WHY 2026-07-27: FilterBar DIREVISI di hari yang sama — semua chip ditampilin sekaligus (dikelompokin + dikasih label) itu SALAH, dan Ray yang nangkep begitu diliat: /modal-gratis jadi 23 kotak putih bergaris sekaligus, / jadi 10, /opensource 18. Ngelompokin cuma NGERAPIHIN tembok, bukan ngurangin bebannya — dan ironisnya critique gw sendiri udah nandain "minimal choices" GAGAL, terus fix-nya malah bikin chip-nya makin menonjol. Cacat aslinya: emphasis KEBALIK. Opsi yang BELUM dipilih teriak paling kenceng (23 kotak dengan garis + fill), yang UDAH dipilih justru kalem. Praktik faceted search yang mapan kebalikannya. Sekarang: istirahat = search + SATU tombol `Filter` + sort; aktif = pil yang bisa dicopot satu-satu + Reset, selalu keliatan tanpa buka panel; panel = grup chip lengkap, cuma pas diminta. Diukur di HTML hasil build: chip terlihat saat istirahat 23/18/12/10 -> 0/0/0/0 di empat permukaan. Panel sengaja DISCLOSURE di alur normal, BUKAN popover — nol perhitungan posisi dan ga bisa ke-clip ancestor overflow-hidden (jebakan yang ditulis DESIGN.md), plus di-mount/unmount biar ga kena kelas bug INCIDENT menu mobile. Catatan: DB ui-ux-pro-max ga punya aturan faceted filter sama sekali (4 query, nol hasil), jadi ini dari praktik mapan, bukan dari database — disebut terang-terangan biar ga keliatan lebih berdasar dari yang sebenernya.
